# Header Search Tabs Light Theme

- pass: Header search tabs render with light-mode segmented styles.
- computed styles: theme `light`, tab background `rgba(255, 255, 255, 0.88)`, active background `rgba(10, 10, 10, 0.08)`, active text `rgb(23, 23, 25)`.
- local runner note: `proof:ui` external Crabbox provider failed its binary sanity check, so this screenshot was captured locally from the worktree with Playwright against `http://127.0.0.1:3027/skills`.
